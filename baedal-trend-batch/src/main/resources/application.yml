spring:
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/domain
    username: sa
    password:


---
spring:
  config:
    activate:
      on-profile: prod


